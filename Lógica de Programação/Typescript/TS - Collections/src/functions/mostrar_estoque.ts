/**
 * Função responsável a realizar o output dos itens adicionados,
 * armazenando-os em um vetor para exibição de todos os itens 
 */
export function mostrar_estoque (estoque: string[]) {
    console.clear ();
    console.log (estoque);
}